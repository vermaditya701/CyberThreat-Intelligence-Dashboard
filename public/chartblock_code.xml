{/* Charts */}
<div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
  <div className="bg-zinc-900 p-4 rounded-xl">
    <h2 className="text-xl font-semibold mb-2">📈 Threats Over Time</h2>
    <ResponsiveContainer width="100%" height={300}>
      <LineChart data={filteredChartData}>
        <Line type="monotone" dataKey="count" stroke="#0f0" strokeWidth={2} />
        <XAxis dataKey="time" stroke="#0f0" />
        <YAxis stroke="#0f0" />
        <Tooltip />
      </LineChart>
    </ResponsiveContainer>
  </div>

  <div className="bg-zinc-900 p-4 rounded-xl">
    <h2 className="text-xl font-semibold mb-2">📊 Attack Distribution</h2>
    <ResponsiveContainer width="100%" height={300}>
      <PieChart>
        <Pie data={attackDistribution} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={80}>
          {attackDistribution.map((_, index) => (
            <Cell key={`cell-${index}`} fill={COLORS[index % COLORS.length]} />
          ))}
        </Pie>
        <Tooltip />
      </PieChart>
    </ResponsiveContainer>
  </div>
</div>
